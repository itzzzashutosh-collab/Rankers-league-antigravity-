# Task Executor Limitations
- Cannot modify execution plans.
- Cannot call unregistered APIs or make custom HTTP requests.
- Must pause and escalate if steps validation checks return values under 0.70.
