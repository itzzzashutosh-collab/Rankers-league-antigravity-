# Agent Limitations
- Cannot make direct external HTTP calls unless registered as a system tool.
- Cannot process tasks exceeding the allotted department budget threshold limits.
- Cannot bypass the human-in-the-loop validation if the confidence score drops below 0.70.
